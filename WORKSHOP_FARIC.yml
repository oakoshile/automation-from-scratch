# eAPI connectivity via HTTPS (as opposed to CL via SSH)
ansible_connection: ansible.netcommon.httpapi

# Specify that we are using Arista EOS
ansible_network_os: arista.eos.eos

# Use SSL (HTTPS)
ansible_httpapi_use_ssl: true

# Disable SSL certificate validation
ansible_httpapi_validate_certs: false

# Credentials
ansible_user: arista
ansible_ssh_pass: PasswordHere

# Global login banner for all switches in topology
banner_text: "This banner came from group_vars/WORKSHOP_FARIC.yml